Делегат "Error" реализован в заголовочном файле Human.h
в сеттере с возрастом, т.к. он не может быть отрицательным.

Работа с исключениями throw, try, catch произведена в Human.h
в сеттере с зарплатой (salary), т.к. она не может быть отрицательной.

Дружественные функции не особо понял, поэтому не использовал.